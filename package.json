{
  "private": true,
  "scripts": {
    "lint": "npx prettier --check tests/",
    "lint:fix": "npx prettier --write tests/",
    "test": "npx anchor test",
    "deploy": "npx anchor deploy --provider.cluster devnet"
  },
  "devDependencies": {
    "@solana/wallet-adapter-base": "^0.6.0",
    "@project-serum/anchor": "=0.25.0",
    "@project-serum/anchor-cli": "=0.25.0",
    "mocha": "^9.1.3",
    "rxjs": "^7.3.0",
    "prettier": "^2.5.1"
  }
}
